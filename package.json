{
  "name": "leadforge-ai",
  "version": "0.1.0",
  "private": true,
  "description": "LeadForge AI — root workspace. The actual code lives in /backend and /frontend.",
  "scripts": {
    "tunnel": "bash scripts/tunnel-cf.sh",
    "tunnel:ngrok": "bash scripts/tunnel.sh",
    "tunnel:stop": "pkill -x ngrok 2>/dev/null; pkill -x cloudflared 2>/dev/null; true",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:workers": "cd backend && npx tsx watch src/workers/index.ts",
    "doctor": "bash scripts/doctor.sh"
  }
}
